Kimchi-ginger
==============

Kimchi is an HTML5 based management tool for KVM. It is designed to make it as
easy as possible to get started with KVM and create your first guest.

Kimchi-ginger is a plugin for Kimchi for managing host. Current features
include user login account management, network interface configuration,
configuration backup, firmware update.
